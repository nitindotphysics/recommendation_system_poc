# Recommendation App (Content-based, User-User CF, Item-Item CF, SVD)

Wraps your Rec_Final.py and the three cleaned CSVs. Titles-only outputs.

## Run
```
pip install -r requirements.txt
python app.py
```
